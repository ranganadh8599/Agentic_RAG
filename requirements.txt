# Agentic RAG - dependencies
# Install with:  pip install -r requirements.txt

# Multi-provider LLM + embeddings (OpenAI / Gemini / Claude / Ollama / ...)
litellm>=1.40
google-generativeai>=0.7

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# PostgreSQL driver + vectors
psycopg[binary]>=3.1
# Connection pooling (used by db.get_conn() when available; safe to omit —
# the code falls back to per-call connections).
psycopg-pool>=3.1

# MongoDB (users, sessions, chat history)
pymongo>=4.6

# Document loading
pypdf>=4.0
pillow>=10.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23

# Utilities
python-dotenv>=1.0
numpy>=1.26

# Cross-encoder reranker (two-stage retrieval). Optional: without it the
# pipeline falls back to a lightweight lexical reranker.
sentence-transformers>=3.0
# For GPU (CUDA) reranking install the CUDA build instead of the default wheel:
#   pip install --force-reinstall torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.2

# RAG evaluation (end-to-end quality: faithfulness, answer relevancy,
# context precision/recall). Used by eval_ragas.py.
ragas>=0.3
langchain-litellm>=0.3
# Pin fsspec so ragas/datasets stays compatible (torch's CUDA wheels bump it
# past the version datasets allows).
fsspec<=2026.6.0

# --- Optional dev-only tools (not required by the server) ---
# make_fixtures.py fixture generator
reportlab>=4.0
