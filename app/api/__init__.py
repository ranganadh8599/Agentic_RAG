# Agentic RAG - FastAPI routes & dependencies.
#
# The application is assembled in app.main (uvicorn app.main:app). Each route
# module exposes an `APIRouter`:
#   routes_health, routes_documents, routes_collections, routes_auth,
#   routes_conversations, routes_chat
