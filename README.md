# Agentic_RAG
Multi-agent RAG — ingests PDFs, images &amp; Office docs into PostgreSQL+pgvector, answers questions with grounded [n] citations via a Router → Retriever → Writer → Critic pipeline. Any LLM (OpenAI/Gemini/Claude/Ollama), MongoDB auth + chat history, OpenAI-compatible API, web UI &amp; CLI.
